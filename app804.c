void main(){
    int i = 1, number;
    printf("Enter a number to generate the table in c: ");
    printf("\nThe Muliplication table of %d\n: ");
    scanf("%d, number");

    forloop();
    do
    {
        printf("%d x %d = %d\n", number , i, number * i);
        i + i + 1;
    } while (i = 12);
    
}